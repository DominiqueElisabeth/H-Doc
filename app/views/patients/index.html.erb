
<div class="mb-4">
  <h3><%= t('view.patient') + t('view.index') %></h3>
</div>

<div class="d-flex flex-column justify-content-center patient_table_wrapper container">
  <div class="row mt-1">
    <div class="col-4 patient_col col_top"><%= t('view.name') %></div>
    <div class="col-4 patient_col col_top"><%= t('view.size') %></div>
    <div class="col-4 patient_col col_top"><%= t('button') %></div>
  </div>
  <% @patients.each do |patient| %>
    <% if patient.posts.present? %>
      <div class="row mt-2">
        <div class="col-4 patient_col"><%= link_to patient.name, post_path(patient.posts.last.id) %></div>
        <div class="col-4 patient_col"><%= patient.posts.size %></div>
        <div class="col-4 patient_col"><button type="button" class="btn btn-danger"><%= link_to t('view.destroy'), patient, method: :delete,
                  data: { confirm: t('confirm.delete') } %></button></div>
      </div>
    <% end %>
  <% end %>
</div>
<%= paginate(@patients) %>
