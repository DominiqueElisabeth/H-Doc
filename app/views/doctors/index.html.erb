<h1>Healthcare Doctor List</h1><h3>Number of Doctors: <%= @doctors.count %></h3>

<%= link_to 'Add Doctor', new_doctor_path %>

<div class="d-flex flex-column justify-content-center patient_table_wrapper container">
  <div class="row mt-1">
    <div class="col-4 patient_col col_top"><%= 'Doctors/Appointment' %></div>
    <div class="col-4 patient_col col_top"><%= 'Admin/Not' %></div>
    <div class="col-4 patient_col col_top"><%= '' %></div>
  </div>

  <% @doctor.each do |doctor| %>
      <div class="row mt-2">
        <div class="col-4 patient_col"><%= link_to doctor.name, doctor_appointments_path(doctor), class: "ui basic button" %>
        <%= doctor.appointments.count %></div>
        <div class="adminmark col-4 patient_col"><%= doctor.admin == true ? "○" : "" %></div>
        <div class="col-4 patient_col d-flex flex-row justify-content-center">
          <button type="button" class="btn btn-outline-info mx-1"><%= link_to 'Edit', edit_doctor_path(doctor) %></button>
          <button type="button" class="btn btn-danger mx-1">
          <%= link_to 'delete', doctor, method: :delete,
                data: { confirm: 'delete' } %></button>
        </div>
      </div>
    <% end %>
</div>
